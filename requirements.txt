yfinance>=1.5.1
pandas==2.2.2
numpy==2.4.6
requests==2.34.2
python-dotenv==1.2.2
pyyaml==6.0.3
jinja2==3.1.6
kap_client>=1.1.1
pydantic>=2.13.4
schedule==1.2.2
tabulate==0.10.0
beautifulsoup4>=4.15.0
anthropic>=0.120.0
pytest>=9.1.1
pytest-cov>=7.1.0
nltk>=3.10.0
pyarrow>=25.0.0
scipy>=1.17.1
statsmodels>=0.14.6    # D-139 BH-FDR multiple-testing correction (ICCalculator.compute_fdr_panel)
playwright>=1.61.0    # D-116 Fintables takas scraper (opsiyonel; post-install: playwright install chromium)
playwright-stealth>=2.0.3  # D-116 bot-detection bypass (playwright ile birlikte kurulur)
hmmlearn>=0.3.3       # D-123 HMM regime-conditional weights
scikit-learn>=1.9.0   # D-123 StandardScaler (hmmlearn dependency, explicit for clarity)
pdfplumber>=0.11.10    # D-132 Is Yatirim aciga satis PDF parser
openpyxl>=3.1.5       # D-203 degoran fundamentals .xlsx parser (pd.read_excel engine)
arch>=8.0.0,<9         # RR-Y1-022 MTH (Reality Check / SPA bootstrap); floor 8.0 - golden byte-reproduction re-validated on 8.0.0 (12/12 golden identical); older majors can silently shift the RC anchor; capped <9
import-linter>=2.13    # D-138 architecture/layering contracts (CI-enforced)
